{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": ["cd backend && python manage.py migrate"],
    "startCommand": "cd backend && python manage.py runserver 0.0.0.0:$PORT --noreload",
    "restartPolicyType": "ON_FAILURE"
  }
}