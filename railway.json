{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "command": "cd backend && python3 -m pip install -r requirements.txt && python3 manage.py migrate && python3 manage.py collectstatic --noinput"
  },
  "start": {
    "command": "cd backend && gunicorn camera101.wsgi:application --bind 0.0.0.0:$PORT"
  }
}